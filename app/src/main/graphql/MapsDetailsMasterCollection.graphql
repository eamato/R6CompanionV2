query MapsDetailsMasterCollection {
    mapsDetailsMasterCollection {
        total
        skip
        limit
        items {
            slug
            sys {
                id
            }
            mapThumbnail {
                url
            }
        }
    }
}